torch>=2.1
transformers>=4.40
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10
matplotlib>=3.7    # only needed for --graph
sae-lens>=6.0        # sae_layer_profile / run_sae_decompose (Tier L/C)
transformer_lens      # only used inside tools/sae.py, to source activations
                       # that match the SAE's training distribution

# optional, only needed for the corresponding LLM_BACKEND:
# accelerate           # AMI_LLM_BACKEND=hf_local on GPU / multi-device
# bitsandbytes         # AMI_LLM_BACKEND=hf_local with 4-bit quantization
# openai               # AMI_LLM_BACKEND=openai
# anthropic            # AMI_LLM_BACKEND=anthropic
