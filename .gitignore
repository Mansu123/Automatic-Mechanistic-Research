__pycache__/
*.pyc
.DS_Store

# model weights are downloaded to ~/.cache/huggingface, never into this repo,
# but these patterns are here defensively in case anything gets saved locally
*.bin
*.safetensors
*.pt
*.pth
*.gguf
*.ckpt

# local venvs, if anyone creates one here
.venv/
venv/

# finetune_stand_in.py saves to ~/.cache/automechinterp/ by default, outside
# this repo -- this entry is defensive only, in case that default is ever
# pointed at a path inside the project
.cache/
gpt2-biomedical-stand-in/

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
